<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :project_title %><br />
    <%= f.text_field :project_title %>
  </div>
  <div class="field">
    <%= f.label :project_note %><br />
    <%= f.text_area :project_note %>
  </div>
  <div class="field">
    <%= f.label :project_locked %><br />
    <%= f.check_box :project_locked %>
  </div>
  <div class="field">
    <%= f.label :dmptemplate_id %><br />
    <%= f.number_field :dmptemplate_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
