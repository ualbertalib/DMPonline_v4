<%
  app_link = link_to t('devise.mailer.app_name'), root_url
  confirmation_link = link_to t('devise.mailer.confirmation_instructions.link'), confirmation_url(@resource, :confirmation_token => @token, :locale => I18n.locale)
  confirmation_direct_url = confirmation_url(@resource, :confirmation_token => @token, :locale => I18n.locale)
%>
<%= raw t('devise.mailer.confirmation_instructions.email', :email => @email, :app_link => app_link, :confirmation_link => confirmation_link, :confirmation_direct_url => confirmation_direct_url) %>
