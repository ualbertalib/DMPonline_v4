<p>Welcome to DMP Builder, <%= @email %>!</p>

<p>Thank you for registering at <%= link_to 'DMP Builder', root_url %>. Please confirm your email address:</p>

<p><%= link_to 'Click here to confirm your account', confirmation_url(@resource, :confirmation_token => @resource.confirmation_token) %> (or copy <%= confirmation_url(@resource, :confirmation_token => @resource.confirmation_token) %> into your browser).</p>
