<%
  resource_email = @resource.email
  app_link = link_to t('devise.mailer.app_name'), root_url
  reset_password_link = link_to t('devise.mailer.reset_password_instructions.link'), edit_password_url(@resource, :reset_password_token => @token)
%>
<%= raw t('devise.mailer.reset_password_instructions.email', :resource_email => resource_email, :app_link => app_link, :reset_password_link => reset_password_link) %>
