<%
  resource_email = @resource.email
  app_link = link_to "DMP Builder", root_url
  unlock_link = link_to 'Unlock my account', unlock_url(@resource, :unlock_token => @token)
%>

<%= raw t('devise.mailer.unlock_instructions.email', :resource_email => resource_email, :app_link => app_link, :unlock_link => unlock_link) %>
