<h1>Editing plan</h1>

<% @plan.version.sections.each do |section| %>
	<% section.questions.each do |question| %>
  	<p><%= question.text %></p>
  <% end %>
<% end %>

<%= link_to 'Show', @plan %> |
<%= link_to 'Back', plans_path %>
