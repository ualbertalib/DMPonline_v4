<%= form_for(@user_type) do |f| %>
  <% if @user_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_type.errors.count, "error") %> prohibited this user_type from being saved:</h2>

      <ul>
      <% @user_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_type_name %><br />
    <%= f.text_field :user_type_name %>
  </div>
  <div class="field">
    <%= f.label :user_type_desc %><br />
    <%= f.text_area :user_type_desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
